server:
  port: 8020
spring:
  application:
    name: consumer
eureka:  #注册到注册中心的微服务
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true