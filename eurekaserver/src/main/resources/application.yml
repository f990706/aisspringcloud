server:
  port: 8761   #注册中心默认端口
eureka:
  client:
    register-with-eureka: false #是否要注册他，是否要注册自己（因为其本身也是一个微服务）
    fetch-registry: false #是否同步其他注册中心的数据
    service-url: #注册中心访问地址
      defaultZone: http://localhost:8761/eureka/ #默认路径